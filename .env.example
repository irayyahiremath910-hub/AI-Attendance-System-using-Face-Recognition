# Django Security Configuration
# Generate a new SECRET_KEY: python manage.py shell -> from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())
SECRET_KEY=your-secret-key-here

# Debug mode - MUST be False in production
DEBUG=False

# Allowed hosts - comma-separated, add your domain names
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database Configuration - Use PostgreSQL in production
# For Development: Use SQLite (django.db.backends.sqlite3)
# For Production: Use PostgreSQL (django.db.backends.postgresql)

# Development Database (SQLite)
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3

# Production Database (PostgreSQL) - RECOMMENDED
DB_ENGINE=django.db.backends.postgresql
DB_NAME=ai_attendance_db
DB_USER=postgres
DB_PASSWORD=your-secure-password-here
DB_HOST=localhost
DB_PORT=5432

# PostgreSQL Connection Pool Settings (optional)
# Controls connection pool size and timeout (in seconds)
DB_CONN_MAX_AGE=600
DATABASE_CONNECTION_POOL_SIZE=10
DATABASE_POOL_TIMEOUT=30

# Application Settings
TIME_ZONE=Asia/Riyadh
LANGUAGE_CODE=en-us
USE_I18N=True
USE_TZ=True

# Security Headers
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_SECURITY_POLICY=default-src 'self'
X_FRAME_OPTIONS=DENY

# Static and Media Files
STATIC_URL=/static/
STATIC_ROOT=staticfiles/
MEDIA_URL=/media/
MEDIA_ROOT=media/

# AWS S3 Configuration (if using cloud storage)
USE_S3=False
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=us-east-1

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Redis Cache Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Celery Configuration (if using async tasks)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Logging
LOG_LEVEL=INFO

# API Rate Limiting
RATELIMIT_ENABLE=True
RATELIMIT_USE_CACHE=cache

# Face Recognition
FACE_RECOGNITION_MODEL=hog
MIN_FACE_CONFIDENCE=0.6

# Django Admin
DJANGO_ADMIN_URL=admin/

# Database Management Commands (For reference, not env variables)
# Check database connection: python manage.py shell -> from Project101.db_utils import check_database_connection; print(check_database_connection())
# Run migrations: python manage.py migrate
# Create new migrations: python manage.py makemigrations
# View migration status: python manage.py showmigrations
# View database stats: python manage.py shell -> from Project101.db_utils import get_database_stats; print(get_database_stats())
# Backup database: python manage.py shell -> from Project101.db_utils import backup_database; print(backup_database())
# Optimize database: python manage.py shell -> from Project101.db_utils import optimize_database; print(optimize_database())

# Media Files
MEDIA_URL=/media/
MEDIA_ROOT=media/

# Static Files
STATIC_URL=/static/

# Email Configuration (Optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Logging
LOG_LEVEL=INFO
